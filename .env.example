# Copy to .env.local and fill in from your Supabase project settings
# (Project Settings → API). Both are safe to expose client-side — this is
# the anon/publishable key, not a service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
